import { describe, it, expect } from 'vitest'
import { LanguageIcon } from './LanguageIcon'

describe('LanguageIcon', () => {
    it('should export LanguageIcon component', () => {
        expect(LanguageIcon).toBeDefined()
    })
})
