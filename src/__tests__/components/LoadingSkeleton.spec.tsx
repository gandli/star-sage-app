import { describe, it, expect } from 'vitest'
import { LoadingSkeleton } from './LoadingSkeleton'

describe('LoadingSkeleton', () => {
    it('should export LoadingSkeleton component', () => {
        expect(LoadingSkeleton).toBeDefined()
    })
})
