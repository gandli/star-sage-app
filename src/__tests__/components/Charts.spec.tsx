import { describe, it, expect } from 'vitest'
import Charts from './Charts'

describe('Charts', () => {
    it('should export Charts component', () => {
        expect(Charts).toBeDefined()
    })
})
